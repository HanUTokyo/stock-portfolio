# The browser uses the same-origin /api path by default.
VITE_API_BASE_URL=/api

# Development and preview proxy target. Do not put bearer tokens here.
VITE_DEV_API_PROXY_TARGET=http://localhost:8080
